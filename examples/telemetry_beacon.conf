# sx1262-telemetry-beacon configuration
# One argument per line (argparse fromfile format: flag then value on next line).
#
# Install to ~/.reticulum/telemetry_beacon.conf and it will be loaded
# automatically — no extra flags needed.
#
# Usage:
#   cp examples/telemetry_beacon.conf ~/.reticulum/telemetry_beacon.conf
#   # edit it, then run:
#   sx1262-telemetry-beacon
#   # or pass a custom path explicitly:
#   sx1262-telemetry-beacon --config /path/to/other.conf

--lat
37.7749
--lon
-122.4194
--alt
50
--name
MyPi
--collector
REPLACE_WITH_32_HEX_CHAR_COLLECTOR_HASH
--interval
300
